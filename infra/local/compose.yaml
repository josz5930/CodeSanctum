services:
  postgres:
    image: postgres:17.2-alpine
    environment:
      POSTGRES_USER: codeattest_migrator
      POSTGRES_PASSWORD: synthetic_demo_local_only
      POSTGRES_DB: codeattest
    ports:
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U codeattest_migrator -d codeattest"]
      interval: 2s
      timeout: 3s
      retries: 30
